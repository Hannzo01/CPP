#ifndef Base_HPP
#define Base_HPP

#include <iostream>


class Base
{
	private:
		// Base();
		// Base(const Base& other);
		// Base& operator=(const Base& other);
	public:
		virtual ~Base();

};

#endif